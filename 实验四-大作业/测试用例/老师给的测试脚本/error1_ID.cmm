/**
 * 标识符测试
 * 
 * 2016-12-26
 * 
 * 顾岩、朱雨晨
 *
 * 测试变量声明是否合法 主要是：
 * 1.标识符是否满足 用例规范(以字母、
 *   数字、下划线组成，只能以字母开头
 *   ，且不能以下划线结束)
 * 2.在相同作用域内不允许出现同名变量
 */


//测试标识符是否合法
int a;
double a;	//数字开头
int a_;		//下划线结尾
int a@com;	//非法字符

int i=0;
while(i<6)
{
	write("Hello world!",i);
	i=i+1;
}

//相同作用域内定义同名变量
int i=10;

/*output
报错：报错信息可能各异
*///:~